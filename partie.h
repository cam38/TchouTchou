#ifndef PARTIE_H
#define PARTIE_H

#include <QMainWindow>
#include <QObject>
#include <QWidget>

#include "iostream"

using namespace std ;


class partie
{
public:
    partie();
/*
private :
    bool resultat ;
    string niveau ;
    int nbCase ; //utile ?
    bool partieEnCours ;

public:
    partie();
    // constructeur de recopie/destructeur ?

    //changer les arguments
    void afficher(void);
    bool cheminValide(grille g);
    bool continuer(void);
    bool lancerTrain(void);
    string trajectoire(void);
    void appuyerFeu(void);
*/
};

#endif // PARTIE_H
